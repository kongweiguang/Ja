# @author kongweiguang

# Desktop and CLI share the workspace target tree; neither application owns
# the other's build output. Explicit isolated target directories remain supported.
[build]
target-dir = "target"
