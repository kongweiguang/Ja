<!-- @author kongweiguang -->

## 改进

- 内置文件工具以 `fd` 与 `rg` 的原生匹配和忽略语义提供受限的读取、目录列举、文件发现和内容搜索，并将清晰、可纠正的参数错误结果返回给 Agent。
- 工具执行过程采用紧凑的动作、工具名、目标与状态展示；文件、目录、搜索命中和读取结果使用适合扫描的结构化视图，同时保留未知结果的完整原文回退。

## 修复

- 修复工具调用、错误详情与历史回放之间的投影不一致，确保失败调用自动展开、成功调用按需展开，并在会话重新载入后保持顺序、身份和公开诊断。
